# Credenciales de MySQL. En un proyecto real este archivo NO se versiona
# en Git con la clave en texto plano; se deja aqui solo para fines
# academicos. Los valores van en base64 (echo -n "valor" | base64).
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: devops-usuarios
type: Opaque
data:
  # root-2023
  MYSQL_ROOT_PASSWORD: cm9vdC0yMDIz
  # usuariosdb
  MYSQL_DATABASE: dXN1YXJpb3NkYg==
