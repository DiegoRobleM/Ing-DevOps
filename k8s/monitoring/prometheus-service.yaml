# http://<IP_PUBLICA_EC2>:30090 para entrar a la UI de Prometheus.
apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: devops-usuarios
spec:
  type: NodePort
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30090
